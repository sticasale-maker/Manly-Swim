# Hook scripts must stay LF. This clone runs core.autocrlf=true, which would
# otherwise check them out CRLF, and Git Bash cannot run a script whose shebang
# line ends in CR — it reports "/bin/sh^M: bad interpreter" and the hook silently
# does nothing, which for a drift guard is worse than not having one.
tools/hooks/** text eol=lf
