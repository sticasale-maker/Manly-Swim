name = "push-bluebottle"
main = "push-bluebottle.js"
compatibility_date = "2026-07-20"

# No bindings needed. All config is delivered as secrets:
#   WEBHOOK_SECRET, VAPID_PUBLIC_KEY, VAPID_PRIVATE_KEY, VAPID_SUBJECT,
#   SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY
# Set each with:  npx wrangler secret put <NAME>
